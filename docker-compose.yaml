services:
  frontend:
    container_name: angels-phonebook-fe-app
    hostname: angels-phonebook-fe-app
    image: belshed/angels-phonebook-fe-app:latest
    pull_policy: always
    networks:
      - gateway-network
    depends_on:
      - backend
    entrypoint:
      - serve
      - -s
      - dist
      - -l
      - "3000"
    env_file:
      - .env
  backend:
    container_name: angels-phonebook-be-app
    hostname: angels-phonebook-be-app
    image: belshed/angels-phonebook-be-app:latest
    pull_policy: always
    networks:
      - gateway-network
    entrypoint:
      - node
      - dist/main.js
    env_file:
      - .env

networks:
  gateway-network:
    driver: bridge
    external: true
    name: gateway-network
